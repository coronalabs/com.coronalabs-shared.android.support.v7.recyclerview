Sources for the plugin `shared.android.support.v7.recyclerview`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.android.support.v7.recyclerview" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
